[pytest]
# Put the backend root on sys.path so `import app...` resolves under a bare
# `pytest` invocation (CI runs `pytest tests/`, which — unlike `python -m
# pytest` — does not add the CWD to sys.path). pytest >= 7 native option.
pythonpath = .
testpaths = tests
